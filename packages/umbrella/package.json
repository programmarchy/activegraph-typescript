{
  "name": "activegraph",
  "version": "1.0.5-alpha.0",
  "description": "Active Graph: event-sourced reactive graph runtime. Umbrella package re-exporting core+runtime+store-memory+store-sqlite+packs+pack-diligence+cli.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20.10.0"
  },
  "dependencies": {
    "@activegraph/cli": "workspace:^",
    "@activegraph/core": "workspace:^",
    "@activegraph/pack-diligence": "workspace:^",
    "@activegraph/packs": "workspace:^",
    "@activegraph/runtime": "workspace:^",
    "@activegraph/store-memory": "workspace:^"
  }
}
